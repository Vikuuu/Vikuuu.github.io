---
import { en } from "~/config"

const { home, projects, archive, about, writings } = en.navigation
---

<div
    class="mt-4 mb-10 flex gap-4 overflow-x-auto text-lg whitespace-nowrap [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
>
    {
        home && (
            <a
                href={`/`}
                class="hover:underline hover:underline-offset-4"
                aria-label={"../home"}
                title={"../home"}
                data-astro-prefetch="viewport"
            >
                <p>{"../home"}</p>
            </a>
        )
    }
    {
        projects && (
            <a
                href={`/projects`}
                class="hover:underline hover:underline-offset-4"
                aria-label={"../projects"}
                title={"../projects"}
                data-astro-prefetch="viewport"
            >
                <p>{"../projects"}</p>
            </a>
        )
    }
    {
        archive && (
            <a
                href={`/archive`}
                class="hover:underline hover:underline-offset-4"
                aria-label={"../archive"}
                title={"../archive"}
                data-astro-prefetch="viewport"
            >
                <p>{"../archive"}</p>
            </a>
        )
    }
    {
        about && (
            <a
                href={`/about`}
                class="hover:underline hover:underline-offset-4"
                aria-label={"./about"}
                title={"./about"}
                data-astro-prefetch="viewport"
            >
                <p>{"../ about"}</p>
            </a>
        )
    }
    {
        writings && (
            <a
                href={`/writings`}
                class="hover:underline hover:underline-offset-4"
                aria-label={"./writings"}
                title={"./writings"}
                data-astro-prefetch="viewport"
            >
                <p>{"../ writings"}</p>
            </a>
        )
    }
</div>
